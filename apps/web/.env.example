# Base URL of the QAgent API.
QAGENT_API_URL=http://127.0.0.1:8000

# Organization the dashboard reads as. Sent as the X-Org-Id header.
# Replaced by a real session once authentication lands.
QAGENT_ORG_ID=
